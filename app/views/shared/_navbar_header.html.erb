<nav class="navbar navbar-expand-lg navbar-dark static-top header-navbar" role="navigation">

  <!-- Logo -->
  <%= link_to t('doorkeeper.layouts.admin.nav.oauth2_provider'),
      oauth_applications_path, class: 'navbar-brand' %>

  <!-- Navbar toggler -->
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#headerNavbar"
    aria-controls="headerNavbar"
    aria-expanded="false"
    aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <!-- Navbar -->
  <div class="collapse navbar-collapse" id="headerNavbar">
    <ul class="navbar-nav mr-auto">
      <%= content_tag :li, class: "nav-item #{'active' if request.path == root_path}" do %>
        <%= link_to t('doorkeeper.layouts.admin.nav.home'), root_path, class: 'nav-link' %>
      <% end %>
      <%= content_tag :li, class: "nav-item #{'active' if request.path == oauth_applications_path}" do %>
        <%= link_to t('doorkeeper.layouts.admin.nav.applications'), oauth_applications_path, class: 'nav-link' %>
      <% end %>
    </ul>
  </div>

</nav>
